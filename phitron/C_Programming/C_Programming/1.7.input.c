#include<stdio.h>
int  main() {
    // int a, b;
    // char p;

    // 10% 20%

    // scanf("%d%c %d%c", &a, &p, &b, &p);
    // printf("%d%% %d%%", a, b);

    // int a, b;
    // scanf("%d%% %d%%", &a, &b);
    // printf("%d%% %d%%", a, b);

    // 10a 20b

    int a, b;
    scanf("%da %db", &a, &b);
    printf("%da %db", a, b);

    return 0;
}

/*
4 byte
& = address of
 */